#pragma once

// TODO: First script: Use CALL() to check if need to rebuild, do so if needed
//   TODO: Rebuild is necessary if the cache is missing, or if child has 
//         missing cache/is updated
//   TODO: Set cache to nullptr when a change is made
//   TODO: Double-buffer cache (protect against garbage collection)
// TODO: Second script: Jump to up-to-date script
class ScriptBuilder {
};
